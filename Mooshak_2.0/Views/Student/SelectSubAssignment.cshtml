@{
    ViewBag.Title = "Select Assignment";
}

<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title></title>
</head>
<body>
    @Html.Partial("_StudentAssignmentsForm")

    <div class="above-main submit">
        <div class="left">
            <h2>@Model.AssignmentName</h2>
            <div class="dropdown">
                <button class="btn btn-default dropdown-toggle" type="button" data-toggle="dropdown">
                    ~ Sub-Assignments ~
                    <span class="caret"></span>
                </button>
                <ul class="dropdown-menu">
                    @foreach (var element in Model.SubAssignments)
                    {
                        <li>@Html.ActionLink(Html.DisplayFor(x => element).ToString(), "SelectSubAssignment", new { subID = element, mainID = Model.AssignmentName, courseID = Model.CourseName, studentID = Model.StudentName })</li>
                    }
                </ul>
            </div>
        </div>

        <div class="right">
            <div class="browse">
                <input type="file">
            </div>
            <div class="below-browse">
                <input type="submit" class="btn btn-default" value="Submit" />
                @Html.ActionLink("Description", "ViewDescription", "Student", new { subID = Model.SubAssignmentName, mainID = Model.AssignmentName, courseID = Model.CourseName }, new { @class = "btn btn-default input" })
            </div>
            <p>Try limit: <input class="tryLimit" value="No limit" /></p>
        </div>
    </div>

    <div class="main">
        <table class="default">
            <thead>
                <tr>
                    <th>#</th>
                    <th>Time</th>
                    <th>Language</th>
                    <th>Result</th>
                    <th>View</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var element in Model.Submissions)
                {
                    int grade = element.Grade;
                    <tr>
                        <td>number</td>
                        <td>@DateTime.Now</td>
                        <td>C#</td>
                        <td>@Html.DisplayFor(x => grade)</td>
                        <td>View</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</body>
</html>
